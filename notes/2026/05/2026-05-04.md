# 2026-05-04 Session Notes

## Scope
- Apply the project-specific AI skills pattern from `python-project-template` to `gentle-site-visitor`.
- Fix CI image ownership/build issues found while validating the PR.
- Merge the PR and record follow-up work for the unavailable Codex review hook.

## PR #22

### Implementation
- Renamed canonical AI skill sources from generic names to `gentle-site-visitor-*` directories and skill manifests.
- Updated `AGENTS.md`, AI skill docs, setup docs, template usage docs, and the mockup helper test path to use the project-specific skill names.
- Generalized hardcoded repository owner references by using `${{ github.repository_owner }}` in GitHub Actions docs/workflows and owner-derived defaults in local CI runner helpers.
- Hardened the CI image build by replacing the Ubuntu plus deadsnakes PPA approach with Python slim base images for Python 3.10, 3.11, and 3.12.
- Fixed the dev dependency floor for `types-PyYAML`.
- Added focused coverage tests for release metadata fallbacks, logging payload precedence, and the main entry point.

### Validation
- Local full test suite passed.
- Local CI coverage command passed with 31 tests and 100% total coverage against the fixed 95% target.
- `pre-commit run --all-files` passed.
- The GitHub CI image workflow built and published `ghcr.io/amendez13/gentle-site-visitor-ci:latest`.
- PR CI passed: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.
- Review-thread GraphQL audit returned no review threads.

### PR / Merge Log
- Branch: `fix/project-specific-ai-skills`
- PR: #22
- Merge commit: `e6de12e`
- External review request `@codex review` did not produce a review within the workflow wait window.
- Added the required decision comment before merge and merged after explicit user approval.

## Operational Follow-Up
- Removed stale globally installed generic `feature-delivery` and `feature-design` skills from local Codex and Claude skill directories so new sessions prefer the project-specific `gentle-site-visitor-*` skills.
- Already-running agent sessions may still show cached old skill discovery until restarted.

## Follow-Up Issues Created
- [followup] #23 Enable Codex review flow for pull requests.

## Dependabot PR Cleanup

### Merged PRs
- #1 `docker/build-push-action` from 6 to 7.
- #2 `actions/github-script` from 8 to 9.
- #3 `docker/login-action` from 3 to 4.
- #4 `mypy` from `>=1.5.0` to `>=1.20.2`.
- #5 `python-dotenv` from `>=1.0.0` to `>=1.2.2`.
- #6 `pytest` from `>=7.4.0` to `>=9.0.3`.
- #7 `pylint` from `>=2.17.0` to `>=4.0.5`.
- #8 `pytest-cov` from `>=4.1.0` to `>=7.1.0`.

### Validation
- Refreshed stale Dependabot branches after the original CI failures were traced to old runs failing before checkout while pulling the CI container image.
- GitHub CI passed for every merged PR: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.
- Resolved dependency-line merge conflicts on #7 and #8; `pre-commit run --all-files` passed before each resolution commit, and commit hooks passed.
- Confirmed no open Dependabot PRs remained after merging #8.

## PR #24 / Issue #11 S1 Browser + Primitives

### Implementation
- Delivered S1 foundation modules under `src/gsv/`: browser primitives, `RateLimiter`, fingerprint helpers, `BrowserManager`, and S1-scoped config dataclasses/loader.
- Added seeded RNG support for low-level human-cadence primitives and viewport generation.
- Added mocked Playwright manager coverage, config loader tests, browser primitive/rate-limit/fingerprint tests, package import smoke coverage, and a reusable local fixture HTTP server for later slices.
- Added `playwright` as a runtime dependency and updated the CI coverage/test jobs to install `requirements-dev.txt` before pytest so PR dependency changes are validated before the shared CI image is rebuilt.
- Updated architecture, implementation-plan, S1 task, CI, and example config docs for the landed browser/config behavior.

### Review Feedback
- Codex review found two S1 config parser issues: unrecognized boolean strings were accepted through Python truthiness, and `api_key: null` became the literal string `None`.
- Fixed both in commit `54a4bd8` by rejecting unrecognized boolean strings with `ConfigError`, preserving defaults for nullable string settings, and adding regression tests.
- Replied inline to every review thread, pushed one hardening commit, reran CI, requested another review, and resolved the outdated review threads.
- The follow-up review request returned a Codex usage-limit notice; no new actionable review feedback arrived.

### Validation
- Local: `pytest --cov=src --cov-report=term-missing --cov-fail-under=95` passed with 82 tests and 99.63% total coverage after review fixes.
- Local: Python 3.12 coverage run passed before the first push; Python 3.10 and 3.11 were not installed locally.
- Local: `pre-commit run --all-files` passed before commit and after review fixes; commit hooks also passed.
- GitHub CI passed after review fixes: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.
- Forbidden-string scan over `src/gsv` and `tests` was clean for `linkedin`, `vps`, `job`, `company`, `search_task`, `es-ES`, and `Europe/Madrid`.

### PR / Merge Log
- Branch: `feature/s01-browser-primitives`
- PR: #24
- Issue closed: #11
- Merge commit: `6d673a0`
- Squash-merged after CI was green, review findings were fixed, decision comment was posted, and review threads were audited/resolved.

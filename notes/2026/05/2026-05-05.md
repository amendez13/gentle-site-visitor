# 2026-05-05 Session Notes

## Scope
- Deliver GitHub issue #13 / S3 pacing through the default PR workflow.
- Deliver GitHub issue #14 / S4 visit runner through the default PR workflow.
- Deliver GitHub issue #15 / S5 observability through the default PR workflow.
- Deliver GitHub issue #16 / S6 CLI through the default PR workflow.
- Deliver GitHub issue #17 / S7 run, lease, and cancel through the default PR workflow.

## PR #31 / Issue #13 S3 Pacing

### Implementation
- Added `gsv.pacing` with `DelayProfile`, `BurstGovernor`, `ContentAwareWait`, the `Pacing` aggregate builder, and public package exports.
- Extended pacing configuration with named delay-profile registries, selected profile, burst cooldown settings, and content-aware wait settings.
- Added default profiles matching the slice contract: `production`, `recon`, `auth`, and `disabled`.
- Replaced S2 auth-flow temporary raw delay calls with an auth `DelayProfile`, while preserving explicit zero-delay test overrides.
- Updated README, architecture, example config, S2 task notes, S3 task notes, and `src/gsv/pacing/README.md`.

### Review Feedback
- Codex review found selected pacing-profile typos were only rejected later by `build_pacing()`; fixed in `cfdaf27` by validating `visitor.pacing.profile` during config loading.
- Codex review found programmatic profile overrides that omitted `auth` could break `Session` initialization; fixed in `cfdaf27` by falling back to the built-in auth profile and adding a regression test.
- Replied inline to both review threads, pushed one hardening commit, reran validation and CI, requested another review pass, and resolved the outdated review threads.

### Validation
- Local: `venv/bin/pytest` passed with 137 tests.
- Local: `venv/bin/pytest tests/pacing --cov=src/gsv/pacing --cov-report=term-missing --cov-fail-under=95` passed with 100% pacing coverage.
- Local: `venv/bin/pre-commit run --all-files` passed before commit and after review fixes; commit hooks also passed.
- GitHub CI passed after review fixes: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.
- Forbidden-string scan over `src/gsv` and `tests` was clean for `linkedin`, `vps`, `job`, `company`, and `search_task`.

### PR / Merge Log
- Branch: `feature/s03-pacing`
- PR: #31
- Issue closed: #13
- Merge commit: `19b75b8`
- Merged after CI was green, review findings were fixed, decision comment was posted, and review threads were audited/resolved.

## PR #32 / Issue #14 S4 Visit Runner

### Implementation
- Added `gsv.visit` with `VisitPlan`, `VisitStep`, `StepResult`, `VisitRunner`, `VisitContext`, evidence sinks, and public package exports.
- Implemented built-in steps for navigation, waiting, click/type/scroll/dwell actions, extraction, branching, per-item iteration, event recording, and explicit burst cooldown/reset.
- Preserved the seven-stage step wrapper contract: cancellation pre-check, rate limit, execute, content-aware wait, delay, burst tick, and cancellation post-check.
- Kept S4 decoupled from later layers by using cancellation and evidence protocols instead of importing `gsv.run`, `gsv.observability`, or `gsv.cli`.
- Updated architecture docs, the S4 task doc status, the visit README, and smoke imports.

### Review Feedback
- Codex review found `ForEach` could return success when a nested iteration subplan failed; fixed in `65385c0` by propagating nested failed/cancelled outcomes into the parent step result.
- Codex review found `BurstCooldown` double-ticked the burst governor when run through `VisitRunner`; fixed in `65385c0` by allowing explicit cooldown steps to opt out of the runner automatic burst tick.
- Replied inline to both review threads, pushed the hardening commit, reran validation and CI, requested another review pass, and resolved the addressed threads after the follow-up review window elapsed without new comments.

### Validation
- Local: `../gentle-site-visitor/venv/bin/pytest` passed with 163 tests.
- Local: `../gentle-site-visitor/venv/bin/pytest tests/visit --cov=src/gsv/visit --cov-report=term-missing --cov-fail-under=90` passed with 97.58% visit coverage.
- Local: `../gentle-site-visitor/venv/bin/mypy src/gsv/visit` passed.
- Local: `../gentle-site-visitor/venv/bin/pre-commit run --all-files` passed before the initial commit and after review fixes; commit hooks also passed.
- GitHub CI passed after review fixes: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.
- Forbidden-string and layering scans were clean for the S4 scope.

### PR / Merge Log
- Branch: `feature/s04-visit-runner`
- PR: #32
- Issue closed: #14
- Squash merge commit: `0e3147a`
- Merged after CI was green, review findings were fixed, decision comment was posted, and review threads were audited/resolved.

## PR #33 / Issue #15 S5 Observability

### Implementation
- Added `gsv.observability` with manifest models, recorder lifecycle, session-store listing/inspection helpers, retention planning/enforcement, success cleanup, README docs, and package exports.
- Added browser recording lifecycle support for trace, HAR, and video artifacts, including recorder attachment, artifact registration, HAR/video context rotation, and success-mode cleanup.
- Extended observability config with retention days and max-session defaults.
- Integrated visit execution with recorder-backed counters and evidence events while leaving final manifest writing to outer teardown after browser artifacts are registered.
- Updated architecture docs, the S5 task doc status, smoke imports, and observability documentation.

### Review Feedback
- Codex review found HAR/video context rotation could resample viewport mid-session; fixed in `02d46f7` by preserving the per-session viewport across recorded and restored contexts.
- Codex review found retention `kept_count` undercounted remaining sessions after partial deletion failures; fixed in `02d46f7` by counting only successful deletions in real mode.
- Codex review found `VisitRunner` finalized manifests before trace/HAR/video artifact registration; fixed in `096301f` by snapshotting counters only and documenting caller-owned finalization after teardown.
- Codex review found recording context creation failure after closing the old context could leave a stale closed context; fixed in `096301f` by restoring a baseline context from saved storage state and clearing the manager context if restoration also fails.
- Replied inline to every actionable review thread, including duplicate findings, pushed hardening commits, reran validation and CI, requested another review pass, and resolved all addressed threads after Codex returned no major issues.

### Validation
- Local: `../gentle-site-visitor/venv/bin/pytest` passed with 189 tests.
- Local: `../gentle-site-visitor/venv/bin/pytest tests/observability tests/browser/test_recording.py tests/browser/test_manager.py --cov=gsv.observability --cov=gsv.browser.recording --cov-report=term-missing --cov-fail-under=90` passed with 91.98% scoped coverage.
- Local: `../gentle-site-visitor/venv/bin/mypy src/gsv` passed.
- Local: `../gentle-site-visitor/venv/bin/pre-commit run --all-files` passed; commit hooks also passed.
- GitHub CI passed after review fixes: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.

### PR / Merge Log
- Branch: `feature/s05-observability`
- PR: #33
- Issue closed: #15
- Squash merge commit: `4eac041`
- Merged after CI was green, review findings were fixed, decision comment was posted, and review threads were audited/resolved.

## PR #34 / Issue #16 S6 CLI

### Implementation
- Added the `gsv` Click entrypoint, console-script packaging metadata, and command modules for `run`, `sessions`, `plan`, and `config`.
- Added the `gsv.apps` registry so configured app modules can register site `VisitPlan` factories for `gsv run`.
- Added `gsv run <site>` as a single-shot in-process driver that loads config, restores/authenticates a session, runs a visit plan, registers browser artifacts, and finalizes the session manifest.
- Added `gsv sessions list/open/inspect/purge` over S5 session bundles, including per-site session directory resolution under `visitor.observability.sessions_dir/<site>`.
- Added `gsv config validate` with JSON output and secret redaction, plus an S8 placeholder for `gsv plan show`.
- Updated README, config example, architecture docs, and the S6 task doc.

### Review Feedback
- Requested Codex review after CI was green and posted a self-review covering run lifecycle ordering, exit-code mapping, session paths, secret redaction, and packaging.
- Audited PR review threads with GraphQL after the review window elapsed; no Codex review or actionable review threads arrived within five minutes.
- Posted the required decision comment. A first decision comment was shell-mangled by unescaped backticks, then a corrected decision comment was posted immediately.

### Validation
- Local: `../gentle-site-visitor/venv/bin/pytest` passed with 210 tests.
- Local: `../gentle-site-visitor/venv/bin/pytest tests/cli --cov=src/gsv/cli --cov=src/gsv/apps --cov-report=term-missing --cov-fail-under=85` passed with 89.30% scoped coverage.
- Local: `../gentle-site-visitor/venv/bin/mypy src/gsv` passed.
- Local: `../gentle-site-visitor/venv/bin/pip install -e . && ../gentle-site-visitor/venv/bin/gsv --version` passed.
- Local: `../gentle-site-visitor/venv/bin/pre-commit run --all-files` passed; commit hooks also passed.
- GitHub CI passed: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.

### PR / Merge Log
- Branch: `feature/s06-cli`
- PR: #34
- Issue closed: #16
- Squash merge commit: `2156885`
- Merged after CI was green, the review window elapsed without actionable feedback, the decision comment was posted, and review threads were audited.

### Late Review Follow-Up
- Rechecked PR #34 after late Codex review comments arrived and found two actionable unresolved threads.
- Opened follow-up PR #35 from `fix/s06-review-comments`.
- Fixed `gsv run` error classification so setup-time config/adapter failures still exit 20 while app/runtime `ValueError` exits 1.
- Restored `load_config` to parse only the requested site while keeping `load_all_configs` as the strict full-file validation path.
- Added regression tests for both reviewed behaviors.

### Follow-Up Validation / Merge Log
- Local: `../gentle-site-visitor/venv/bin/pytest tests/cli/test_run.py tests/config/test_loader.py -q` passed with 25 tests.
- Local: `../gentle-site-visitor/venv/bin/mypy src/gsv` passed.
- Local: `../gentle-site-visitor/venv/bin/pytest tests/cli tests/config --cov=src/gsv/cli --cov=src/gsv/apps --cov=src/gsv/config --cov-report=term-missing --cov-fail-under=85` passed with 92.33% scoped coverage.
- Local: `../gentle-site-visitor/venv/bin/pre-commit run --all-files` passed; commit hooks also passed.
- Local: `../gentle-site-visitor/venv/bin/pytest` passed with 212 tests.
- GitHub CI passed on PR #35: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.
- PR #35 squash merge commit: `821d4b9`.

## PR #36 / Issue #17 S7 Run + Lease + Cancel

### Implementation
- Added `gsv.run` with `LeaseClient`, `ControlClient`, `CancellationMonitor`, documented exit codes, and `RunController` for coordinated worker execution.
- Added `gsv worker` and `gsv server dev`, including API-key-protected SQLite dev-server endpoints for lease register/heartbeat/release, run create/claim/status/control/submit, cancellation requests, and cancellation acknowledgements.
- Preserved load-bearing S7 invariants: heartbeat backoff `(5, 15, 30)`, default lease TTL 120 seconds, heartbeat interval 30 seconds, cooperative cancellation boundaries, and partial-result cancellation acknowledgements.
- Wired run cancellation checks into pacing pre-cooldown and added `VisitRunner(propagate_cancellation=True)` for controller-owned cancellation handling without changing the default S4 runner behavior.
- Updated README, CLI README, architecture docs, S7 task status, runtime/server READMEs, dependencies, and aligned the local Black pre-commit pin with CI.

### Review Feedback
- Initial Codex review found failed terminal submit/cancellation-ack calls could still report clean success; fixed by treating rejected terminal writes as runtime failures.
- Codex review found heartbeat failures were detached from worker control flow; fixed by surfacing heartbeat failure during idle polling and active run execution.
- Codex review found heartbeat renewals omitted lease TTL; fixed by sending `lease_ttl_seconds` on heartbeat.
- Codex review found direct claim/submission ownership gaps; fixed by rejecting cross-worker claim takeover and requiring claimed ownership for terminal submit and cancellation ack.
- Codex review found broad `KeyError` auth mapping could misclassify app/runtime bugs; fixed by limiting credential `KeyError` handling to the auth boundary.
- Follow-up Codex review found non-JSON lease API responses and `claim_next` race reporting gaps; fixed by normalizing non-JSON responses into structured failure reasons and using atomic `UPDATE ... RETURNING` for `claim_next`.

### Validation
- Local: `../gentle-site-visitor/venv/bin/pytest tests/run tests/server/dev tests/integration/test_worker_endtoend.py -q` passed with 36 tests after review fixes.
- Local: `../gentle-site-visitor/venv/bin/mypy src/ --ignore-missing-imports --strict --explicit-package-bases` passed.
- Local: `../gentle-site-visitor/venv/bin/pytest tests/ -q --cov=src --cov-report=term-missing --cov-fail-under=95` passed with 253 tests and 95.23% coverage.
- Local: `../gentle-site-visitor/venv/bin/pre-commit run --all-files` passed; commit hooks also passed.
- Forbidden-string scans were clean for `src/gsv`, `tests`, and the S7-specific `src/gsv/run` and `src/gsv/server` scopes.
- GitHub CI passed on the final PR head: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.

### PR / Merge Log
- Branch: `feature/s07-run-lease-cancel`
- PR: #36
- Issue closed: #17
- Squash merge commit: `d4e29e9`
- Merged after CI was green, actionable review feedback was fixed, follow-up review threads were audited, and a final decision comment was posted.

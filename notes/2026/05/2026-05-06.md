# 2026-05-06 Session Notes

## Scope
- Deliver GitHub issue #20 / S10 hardening through the default PR workflow.
- Keep the S10 scope focused and split oversized hardening work into follow-up issues.

## PR #44 / Issue #20 S10 Hardening

### Implementation
- Added per-site rate-limit configuration via `RateLimitConfig` and `sites.<name>.rate_limit`, with fallback to `visitor.pacing.rate_limit_per_hour`.
- Preserved the hourly meaning of `requests_per_hour` when a shorter `window_minutes` smoothing window is configured.
- Added manifest `framework_counters_version: 1` and stable `framework.*` counter copies while preserving existing flat counter keys for compatibility.
- Added `CHANGELOG.md` and updated README, architecture docs, implementation plan, S09 notes, and the S10 task doc to describe the v0 baseline and deferred work.
- Created follow-up issues for oversized S10 work:
  - #42 `[followup] S10 pagination and platform caps`
  - #43 `[followup] S10 integrity probes and per-step pacing controls`
  - #41 `[followup] S9 app contract polish: app auth adapters and trace/HAR interaction`

### Review Feedback
- Codex review found partial site `rate_limit` mappings reset the request cap to 90 instead of inheriting the visitor-wide cap; fixed by passing the resolved visitor cap into site rate-limit parsing and adding a regression test.
- Codex review found shorter `window_minutes` values could turn an hourly cap into a per-window cap; fixed by enforcing both a true one-hour ceiling and a scaled smoothing-window bucket in `RateLimiter`.
- Replied inline to both actionable review threads, resolved them after fixes, reran validation and CI after each amended push, and posted the final decision comment.

### Validation
- Local: `../gentle-site-visitor/venv/bin/pytest tests/config tests/browser/test_manager.py tests/browser/test_rate_limit.py tests/observability tests/cli/test_run.py tests/apps/example -q` passed with 71 tests before review fixes.
- Local: `../gentle-site-visitor/venv/bin/pytest tests/browser/test_rate_limit.py tests/browser/test_manager.py tests/config/test_loader.py -q` passed with 45 tests after review fixes.
- Local: `../gentle-site-visitor/venv/bin/pytest tests/ -q --cov=src --cov-report=term-missing --cov-fail-under=95` passed after the final review fix with 319 tests and 95.12% coverage.
- Local: `../gentle-site-visitor/venv/bin/gsv --config apps/example/config.yaml config validate --site example` passed.
- Local fast example smoke run passed with 3 extracted asteroid evidence rows: Ceres `C`, Vesta `V`, and Pallas `B`.
- Local: `../gentle-site-visitor/venv/bin/pre-commit run --all-files` passed; commit hooks also passed on amended commits.
- GitHub CI passed on the final PR head: lint, coverage, security, config validation, Python 3.10, Python 3.11, Python 3.12, and aggregate status check.

### PR / Merge Log
- Branch: `feature/s10-hardening`
- PR: #44
- Issue closed: #20
- Follow-up issues created: #41, #42, #43
- Squash merge commit: `a0383bc`
- Merged after CI was green, actionable review feedback was fixed, review threads were audited/resolved, and the final decision comment was posted.

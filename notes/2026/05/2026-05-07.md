# 2026-05-07

## Issue 47 - Secret Scanning CI

- Delivered GitHub issue #47 through PR #48.
- Added `.github/workflows/gitleaks.yml`, a dedicated Secret Scanning workflow that installs pinned Gitleaks v8.30.1, verifies the Linux x64 archive SHA-256, scans full git history, uploads redacted SARIF when available, and fails on detected leaks.
- Updated CI and branch-protection documentation/configuration so `Secret Scanning` is listed as a required status check alongside the existing CI jobs.
- Addressed Codex review feedback by adding `scripts/security/run-gitleaks.sh`, a local bootstrap script for Gitleaks v8.30.1 on Linux/macOS x64/arm64 with pinned SHA-256 verification before running the local scan.
- Updated `.gitignore` for the local Gitleaks cache path.
- Validation: local bootstrapped Gitleaks scan passed, branch-protection JSON parsed, pre-commit passed, and GitHub CI plus both Secret Scanning runs passed before merge.
- PR #48 was squash-merged to `main` at commit `039428c`.

## Issue 46 - README CI Badges

- Delivered GitHub issue #46 through PR #49.
- Replaced the README's plain CI workflow link with a GitHub Actions CI status badge for the `main` branch.
- Added a Codecov coverage badge for the `main` branch and URL-encoded the Python version badge's plus sign.
- Validation: pre-commit passed locally; GitHub CI passed through the README-only skip path; both Secret Scanning runs passed.
- Codex review reported no major issues, and the GraphQL review-thread audit found no inline threads.
- PR #49 was squash-merged to `main` at commit `ced4d41`.

## Coverage Badge Follow-up

- Investigated the Codecov `unknown` badge state after PR #49.
- Confirmed `gentle-site-visitor` currently reports as private on GitHub, has no `CODECOV_TOKEN` repository secret, and recent Codecov uploads were non-blocking failures.
- Opened follow-up issue #52 to configure the Codecov GitHub app/token, add the `CODECOV_TOKEN` secret, restore a live Codecov badge, and verify a successful `main` upload.
- Closed issue #50 as superseded after the direction changed from live Codecov setup now to a temporary static badge.
- Delivered PR #51 to replace the README Codecov badge with a static `coverage 95%` badge while issue #52 remains open.
- Validation: `pre-commit run --all-files` passed locally; PR #51 CI passed through the docs-only skip path and Secret Scanning passed.
- The prior Codex review finding on the removed Codecov-required CI changes was replied to and resolved as outdated/not applicable; no new automated review arrived during the wait window.
- PR #51 was squash-merged to `main` at commit `cfc79d8`.
